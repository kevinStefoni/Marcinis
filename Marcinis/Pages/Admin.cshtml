@page
@using Marcinis.Helpers
@using Marcinis.Models
@model AdminModel
@{
    ViewData["Title"] = "Admin";
    IList<Customer>? Customers = SessionHelper.GetObjectFromJson<IList<Customer>>(HttpContext.Session, "customers");
    IList<MenuItem>? Menu = SessionHelper.GetObjectFromJson<IList<MenuItem>>(HttpContext.Session, "menu");
    IList<CustomerOrder>? Orders = SessionHelper.GetObjectFromJson<IList<CustomerOrder>>(HttpContext.Session, "orders");
    IList<DiscountCode>? Discounts = SessionHelper.GetObjectFromJson<IList<DiscountCode>>(HttpContext.Session, "discounts");
}

<meta charset="UTF-8">

<head>
    <link rel="stylesheet" href="/css/admin.css" />
</head>

<h1 id="admin">All Users</h1>

<form method="post">
    <table class="table-fill">
        <tr>
            <th>CustomerID</th>
            <th>FirstName</th>
            <th>LastName</th>
            <th>EmailAddress</th>
            <th>PhoneNumber</th>
            <th>LoginTypeId</th>
            <th>Action</th>
        </tr>

        @if (Customers != null)
        {
            @foreach (var c in Customers)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => c.CustomerId)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => c.FirstName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => c.LastName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => c.LoginCredentials.EmailAddress)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => c.PhoneNumber)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => c.LoginTypeId)
                    </td>
                    <td>
                        <Button class="custom-btn btn-3" asp-page-handler="CustomerEdit" asp-route-bindCustId="@c.CustomerId"> &#9999; </Button>
                        <Button class="custom-btn btn-3" asp-page-handler="CustomerDelete" asp-route-bindCustId="@c.CustomerId"> &#10060; </Button>
                    </td>
                </tr>
            }
        }
    </table>

</form>
<div class="container" style="padding:20px">
    
    <form method="post" class="adminForm" asp-page-handler="AddCustomer" enctype="multipart/form-data">
        <div class="text-danger" asp-validation-summary="ModelOnly"></div>

        <!-- Customer ID -->
        <input type="text" asp-for="cust.CustomerId" class="TextInput" placeholder="Customer ID" />

        <!-- First Name -->
        <input type="text" asp-for="cust.FirstName" class="TextInput" placeholder="First Name"/>

        <!-- Last Name -->
        <input type="text" asp-for="cust.LastName" class="TextInput" placeholder="Last Name"/>

        <!-- Email Address -->
        <input type="text" asp-for="cust.LoginCredentials.EmailAddress" class="TextInput" placeholder="Email Address"/>

        <!-- Phone Number -->
        <input type="text" asp-for="cust.PhoneNumber" class="TextInput" placeholder="Phone Number"/>

        <!-- Password -->
        <input type="password" asp-for="cust.LoginCredentials.Password" class="TextInput" placeholder="Password"/>
        <span asp-validation-for="cust.LoginCredentials.Password" class="text-danger"></span>

        <!-- Login Type ID -->
        <input type="text" asp-for="cust.LoginTypeId" class="TextInput" placeholder="Login Type Id" />

        <!-- submit bt -->
        <input class="submitBT" type="submit" value="Add Customer"/>
    </form>

</div>

<form method="post">
    <table class="table-fill">
        <tr>
            <th>Product ID</th>
            <th>Name</th>
            <th>Description</th>
            <th>Type</th>
            <th>Price</th>
            <th>QOH</th>
            <th>Category</th>
            <th>Action</th>
        </tr>

        @if (Menu != null)
        {
            @foreach (var i in Menu)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => i.PROD_ID)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => i.PROD_NAME)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => i.PROD_DESC)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => i.PROD_TYPE)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => i.PROD_PRICE)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => i.PROD_QOH)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => i.PROD_CATEGORY)
                    </td>
                    <td>
                        <Button class="custom-btn btn-3" asp-page-handler="MenuItemEdit" asp-route-bindItemId="@i.PROD_ID">&#9999;</Button>
                        <Button class="custom-btn btn-3" asp-page-handler="MenuItemDelete" asp-route-bindItemId="@i.PROD_ID">&#10060;</Button>
                    </td>
                </tr>

            }
        }
    </table>

</form>



<div></div>

<div class="container border" style="padding:20px">
    <form method="post" asp-page-handler="AddMenuItem" enctype="multipart/form-data">
        <div class="text-danger" asp-validation-summary="ModelOnly"></div>

        <!-- Product ID -->
        <div class="form-group row" style="margin-bottom:15px">
            <div class="col-1">
                <label asp-for="item.PROD_ID">Product ID:</label>
            </div>
            <div class="col-3">
                <input type="text" asp-for="item.PROD_ID" class="form-control" style="width: 250px; margin: 2px" />
            </div>
        </div>


        <!-- Product Name -->
        <div class="form-group row" style="margin-bottom:15px">
            <div class="col-1">
                <label asp-for="item.PROD_NAME">Product Name:</label>
            </div>
            <div class="col-3">
                <input type="text" asp-for="item.PROD_NAME" class="form-control" style="width: 250px; margin: 2px" />
            </div>
        </div>

        <!-- Product Description -->
        <div class="form-group row" style="margin-bottom:15px">
            <div class="col-1">
                <label asp-for="item.PROD_DESC">Product Description:</label>
            </div>
            <div class="col-3">
                <input type="text" asp-for="item.PROD_DESC" class="form-control" style="width: 250px; margin: 2px" />
            </div>
        </div>

        <!-- Product Type -->
        <div class="form-group row" style="margin-bottom:15px">
            <div class="col-1">
                <label asp-for="item.PROD_TYPE">Product Type:</label>
            </div>
            <div class="col-3">
                <input type="text" asp-for="item.PROD_TYPE" class="form-control" style="width: 250px; margin: 2px" />
            </div>
        </div>

        <!-- Product Price -->
        <div class="form-group row" style="margin-bottom:15px">
            <div class="col-1">
                <label asp-for="item.PROD_PRICE">Product Price:</label>
            </div>
            <div class="col-3">
                <input type="text" asp-for="item.PROD_PRICE" class="form-control" style="width: 250px; margin: 2px" />
            </div>
        </div>

        <!-- Product Quantity on Hand -->
        <div class="form-group row" style="margin-bottom:15px">
            <div class="col-1">
                <label asp-for="item.PROD_QOH">Product Initial Quantity on Hand:</label>
            </div>
            <div class="col-3">
                <input type="text" asp-for="item.PROD_QOH" class="form-control" style="width: 250px; margin: 2px" />
            </div>
        </div>

        <!-- Product Category -->
        <div class="form-group row" style="margin-bottom:15px">
            <div class="col-1">
                <label asp-for="item.PROD_CATEGORY">Product Category:</label>
            </div>
            <div class="col-3">
                <input type="text" asp-for="item.PROD_CATEGORY" class="form-control" style="width: 250px; margin: 2px" />
            </div>
        </div>

        <!-- Product Image -->
        <div class="form-group row" style="margin-bottom:15px">
            <div class="col-1">
                <label asp-for="item.ImageFile">Upload Product Image:</label>
            </div>
            <div class="col-3">
                <input type="file" asp-for="item.ImageFile" class="form-control" style="width: 250px; margin: 2px" />
                <span asp-validation-for="item.ImageFile" class="text-danger"></span>
            </div>
        </div>

        <!-- submit bt -->
        <div class="form-group row">
            <div class="col-1">
                <input type="submit" class="btn btn-primary" value="Add Item" />
            </div>
        </div>

    </form>
</div>

<form method="post">
    <table class="table-fill">
        <tr>
            <th>Order ID</th>
            <th>Customer ID</th>
            <th>CC Number</th>
            <th>CC CVV</th>
            <th>CC Exp Month</th>
            <th>CC Exp Year</th>
            <th>CC Zip Code</th>
            <th>Pickup Time</th>
            <th>Sub Total</th>
            <th>Tax</th>
            <th>Total</th>
            <th>Order Date</th>
            <th>Action</th>
        </tr>

        @if (Orders != null)
        {
            @foreach (var o in Orders)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => o.ORDER_ID)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => o.ORDER_CUST_ID)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => o.ORDER_CREDIT_CARD_NUM)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => o.ORDER_CREDIT_CARD_CVV)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => o.ORDER_CREDIT_CARD_EXP_MONTH)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => o.ORDER_CREDIT_CARD_EXP_YEAR)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => o.ORDER_CREDIT_CARD_ZIP_CODE)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => o.ORDER_PICKUP_TIME)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => o.ORDER_SUBTOTAL)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => o.ORDER_TAX)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => o.ORDER_TOTAL)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => o.ORDER_DATE)
                    </td>
                    <td>
                        <Button class="custom-btn btn-3" asp-page-handler="OrderDelete" asp-route-bindOrderId="@o.ORDER_ID">&#10060;</Button>
                    </td>
                </tr>

            }
        }
    </table>

</form>



<form method="post">
    <table class="table-fill">
        <tr>
            <th>Discount Code ID</th>
            <th>Customer Code</th>
            <th>Percentage Off</th>
            <th>Start Date</th>
            <th>End Date</th>
            <th>Action</th>
        </tr>

        @if (Discounts != null)
        {
            @foreach (var d in Discounts)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => d.DiscountId)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => d.Code)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => d.Percentage)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => d.StartDate)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => d.EndDate)
                    </td>

                    <td>
                        <Button class="custom-btn btn-3" asp-page-handler="DiscountCodeDelete" asp-route-bindDiscountId="@d.DiscountId">&#10060;</Button>
                    </td>
                </tr>

            }
        }
    </table>

</form>


<div class="container border" style="padding:20px">
    <form method="post" asp-page-handler="AddDiscountCode" enctype="multipart/form-data">
        <div class="text-danger" asp-validation-summary="ModelOnly"></div>

        <!-- Discount Code -->
        <div class="form-group row" style="margin-bottom:15px">
            <div class="col-1">
                <label asp-for="disc.Code">Discount Code:</label>
            </div>
            <div class="col-3">
                <input type="text" asp-for="disc.Code" class="form-control" style="width: 250px; margin: 2px" />
            </div>
        </div>

        <!-- Percentage Off -->
        <div class="form-group row" style="margin-bottom:15px">
            <div class="col-1">
                <label asp-for="disc.Percentage">Percentage Off:</label>
            </div>
            <div class="col-3">
                <input type="text" asp-for="disc.Percentage" class="form-control" style="width: 250px; margin: 2px" />
            </div>
        </div>

        <!-- Start Date -->
        <div class="form-group row" style="margin-bottom:15px">
            <div class="col-1">
                <label asp-for="disc.StartDate">Start Date:</label>
            </div>
            <div class="col-3">
                <input type="text" asp-for="disc.StartDate" class="form-control" style="width: 250px; margin: 2px" />
            </div>
        </div>

        <!-- End Date -->
        <div class="form-group row" style="margin-bottom:15px">
            <div class="col-1">
                <label asp-for="disc.EndDate">End Date:</label>
            </div>
            <div class="col-3">
                <input type="text" asp-for="disc.EndDate" class="form-control" style="width: 250px; margin: 2px" />
            </div>
        </div>

        <!-- submit bt -->
        <div class="form-group row">
            <div class="col-1">
                <input type="submit" class="btn btn-primary" value="Add Discount Code" />
            </div>
        </div>

    </form>
</div>